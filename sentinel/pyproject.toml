[project]
name = "sentinel"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiohttp==3.13.5",
    "discord.py==2.7.1",
    "pydantic>=2.0.0,<3.0.0",
]

[tool.ruff]
extend = "../pyproject.toml"
